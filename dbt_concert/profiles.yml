gigwise_analytics:
  target: prod
  outputs:
    prod:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: "{{ env_var('BQ_DATASET_ANALYTICS') }}"
      location: "{{ env_var('GCP_REGION') }}"
      threads: 4
      timeout_seconds: 300
